export const states = {
  COMPLETED: 'completed',
  FAILED: 'failed',
  PROCESSING: 'processing'
}

export const POLL_INTERVAL = 500;
export const HEARTBEAT_INTERVAL = 3000;
export const PRIORITY_FACTOR = 1000;
export const EXPIRE_MS = 3600;
export const LEASE_MS = 10000;